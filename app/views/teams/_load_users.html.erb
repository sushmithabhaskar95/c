
<div class="modal-header">
<div class="modal-window">
<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
<h3 id="myModalLabel">Modal header</h3>
<div class="modal-body">

<%= form_tag load_users_teams_path, method: :get, remote: true do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search",:first_name => nil %>
  </p>
  <% end %>

<table class="table table-bordered" border="1" width="400">
    <tr>
    <th>Captain Name</th>
    <th>Select Captain</th>
    </tr>
    <tr>
  <% @player.each do |f| %>
  <td><%= f.first_name %> </td>
  <td> <%= link_to "Select", "javascript:void(0)", class: "select-caption", data: {id: f.id, name: f.first_name} %>
  </td>
  </tr>
  <% end %>


<div class="modal-footer">
<button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
<button class="btn btn-primary">Save changes</button>

</div>
</div>
</div>

